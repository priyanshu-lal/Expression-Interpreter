# JADE

A programmable interpreter for evaluating over numeric expression. With features like programmable functions,
variable creation, several built-in functions and many more mathematical utilities

## Building the Project
```bash
# Debug
cmake -B build -DCMAKE_BUILD_TYPE=Debug
cmake --build build

# Release
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build
```
